# .antigravity/examples

Put reusable code patterns, reference snippets, and best-practice examples here.

The generator intentionally ships this as a placeholder so project-specific examples
from the template repository do not leak into newly generated projects.
