---
description: 按照 PRP 蓝图逐步实现功能，含验证门控和自修复循环。
---

## Preflight: TARGET_PROJECT 解析与校验

在加载 PRP 并开始实现前，先执行：

1. 从控制面板根目录 `AGENTS.md` 读取 `TARGET_PROJECT`
2. 将其解析为实际代码仓库根目录
3. 校验目录存在，且看起来像真实代码仓库根目录
4. 向用户回显最终解析路径
5. 后续所有代码实现、验证命令、测试和修复都在 `TARGET_PROJECT` 中进行

PRP 文件本身保留在控制面板仓库；代码实现和验证发生在 `TARGET_PROJECT`。

根据已生成的 PRP 蓝图实现功能：

1. **加载 PRP**
   - 读取指定的 PRP 文件（如 `.vibe/artifacts/prp_todo_crud.md`）
   - 理解所有上下文、需求和验证条件
   - 如需补充研究，先完成后再开始编码

2. **结构化思考**
   - 使用 3-5 步结构化思考规划实现方案（如宿主支持，可使用顺序思考工具）
   - 按 PRP 中的任务列表创建 checklist
   - 识别任务间的依赖顺序

3. **逐步实现**
   对 PRP 中的每个任务：
   - 参考 `.vibe/examples/` 中的代码模式
   - 编写代码（遵循 AGENTS.md 中的编码规范）
   - 完成后立即运行对应的验证命令
   - 所有代码编辑与命令执行均在 `TARGET_PROJECT` 中进行

4. **验证门控**（每个任务完成后执行）
   ```
   Level 1: go build ./...         （编译通过）
   Level 2: go vet ./...           （静态检查）
   Level 3: go test -race ./...    （测试通过）
   ```
   - 如果失败：读错误 → 分析根因 → 修复 → 重新验证
   - 如果同一错误 3 次无法修复 → 停下来问用户

5. **最终确认**
   - 重读 PRP，逐条检查 Success Criteria
   - 运行完整验证套件
   - 记录遇到的问题到 `.vibe/artifacts/error_journal.md`
   - 报告完成状态

> 原则：**永远不跳过验证**。即使"看起来应该能用"也要跑一遍。
